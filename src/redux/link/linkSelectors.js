export const selectLinks = (state) => state.link.links;

export const selectPreviewLinks = (state) => state.link.previewLinks;

export const selectReorderedLinks = (state) => state.link.reorderedLinks;

export const selectIsLoading = (state) => state.link.isLoading;
